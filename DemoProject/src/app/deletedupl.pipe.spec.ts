import { DeleteduplPipe } from './deletedupl.pipe';

describe('DeleteduplPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteduplPipe();
    expect(pipe).toBeTruthy();
  });
});
